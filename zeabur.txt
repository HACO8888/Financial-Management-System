# Zeabur 部署配置

# 構建命令（可選，Zeabur 會自動檢測）
# 如果需要安裝依賴，可以設定：
# pip install -r requirements.txt

# 啟動命令
gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120

# 環境變數（在 Zeabur 控制台設定）
# FLASK_ENV=production
# SECRET_KEY=your-secret-key-here
# DATABASE_URL=your-postgresql-url